    cmake_minimum_required(VERSION 3.0.2)
project(rover_description)
find_package(catkin REQUIRED COMPONENTS xacro)
catkin_package()
install(DIRECTORY urdf launch rviz DESTINATION ${CATKIN_PACKAGE_SHARE_DESTINATION})
